{
  "name": "@orbital-stellar/pulse-webhooks",
  "version": "0.1.0",
  "description": "HMAC-signed webhook delivery and verification for Stellar events (Node + edge runtimes).",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/determined-001/orbital_stellar.git",
    "directory": "packages/pulse-webhooks"
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "bin": {
    "orbital": "./bin/orbital"
  },
  "dependencies": {
    "@orbital-stellar/pulse-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.9.3",
    "tsx": "^4.22.4",
    "vite": "^8.0.16",
    "vitest": "^4.1.9"
  }
}
